# ===========================================
# Cấu hình môi trường - Thông tin nhạy cảm
# File này KHÔNG được commit lên Git
# Thêm dòng ".env" vào file .gitignore
# ===========================================

# ===========================================
# Cấu hình Telegram Bot - Gửi cảnh báo
# ===========================================

# Token bot Telegram
# Lấy từ @BotFather trên Telegram
TELEGRAM_BOT_TOKEN=your_telegram_bot_token

# Chat ID của cá nhân hoặc nhóm nhận thông báo
# Cách lấy: Nhắn tin cho bot trước → truy cập
# https://api.telegram.org/bot<TOKEN>/getUpdates
TELEGRAM_CHAT_ID=your_telegram_id

# Bật/tắt gửi cảnh báo qua Telegram
ENABLE_TELEGRAM_ALERTS=your_option


# ===========================================
# Cấu hình kết nối Elasticsearch
# ===========================================

# Địa chỉ Elasticsearch (thay đổi theo môi trường dev/prod)
ES_HOST=your_es_host

# Cổng Elasticsearch
ES_PORT=your_es_port


# ===========================================
# Cấu hình kết nối pfSense
# ===========================================

# IP hoặc hostname pfSense (thường là giao diện quản trị)
PFSENSE_HOST=your_pfsense_host

# Cổng web interface pfSense (443 cho HTTPS, 80 cho HTTP)
PFSENSE_PORT=your_pfsense_port


# ===========================================
# Cấu hình API pfSense (chỉ cần khi bật chặn IP tự động)
# Tạo API token trong pfSense: System > User Manager > API
# ===========================================

PFSENSE_API_KEY=your_api_key_here
PFSENSE_API_SECRET=your_api_secret_here

# Kiểm tra chứng chỉ SSL (đặt false nếu dùng self-signed cert)
PFSENSE_VERIFY_SSL=false


# ===========================================
# Override debug (tuỳ chọn - bật debug riêng ở local)
# ===========================================

# Nếu muốn bật debug chỉ ở máy local, đặt true
# Giá trị này sẽ override app.debug trong config.yaml
APP_DEBUG=your_option
